// Problem: Delete a Node from Doubly Linked List
// Date: February 9, 2026

#include <bits/stdc++.h>
using namespace std;

struct DListNode {
    int val;
    DListNode* prev;
    DListNode* next;
    DListNode(int x) : val(x), prev(nullptr), next(nullptr) {}
};

// TODO: Implement solution
DListNode* deleteNode(DListNode* head, int key) {
    // Delete node with given key from doubly linked list
    return nullptr;
}

int main() {
    // Test cases
    return 0;
}
