// Primality test provides an algorithmic process used to determine whether a given positive integer is a prime number (divisible only by 1 and itself) or a composite number

#include <bits/stdc++.h>
using namespace std;


int main() {
    
    return 0;
}